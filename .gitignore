node_modules
dist
.turbo
.next
.expo
.env*
!.env.example
!.env.dev.example
!.env.production.example
coverage
*.tsbuildinfo
.worktrees
.superpowers
logs/
# Docker / local upload volume at repo root only (do not use bare `uploads/` — it ignores `backend/service/src/uploads/`).
/uploads/
# Local dev uploads written by the backend (receipts, etc.) — user data, never committed.
/backend/service/uploads/
.DS_Store
.cursor/
